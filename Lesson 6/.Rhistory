# Introduction to R
# Week 5
# Assignment by Nava Peter (211311147)
# 1. Creation of Vectors and Merging
vector1= rnorm(15, mean=0, sd=1) # Creates a vector of a standard distribution
vector2= rnorm(15, mean=0, sd=1) #Creates another vector of standard distribution
vectors_1_2= cbind(vector1, vector2) #Binds the two vectors
# 2. Math equations
# Math Equations on the Vectors
print("Vector 1 Multiplication by 2" + vector1 * 2)
View(vectors_1_2)
# Introduction to R
# Week 5
# Assignment by Nava Peter (211311147)
# 1. Creation of Vectors and Merging
vector1= rnorm(15, mean=0, sd=1) # Creates a vector of a standard distribution
vector2= rnorm(15, mean=0, sd=1) #Creates another vector of standard distribution
matriza1= cbind(vector1, vector2) #Binds the two vectors
# 2. Math equations
# Math Equations on the Vectors
print("Vector 1 Multiplication by 2" + vector1 * 2)
print("Matrix 2: Column 1" + vector_col_1_m2)
# Introduction to R
# Week 5
# Assignment by Nava Peter (211311147)
# 1. Creation of Vectors and Merging
vector1= rnorm(15, mean=0, sd=1) # Creates a vector of a standard distribution
vector2= rnorm(15, mean=0, sd=1) #Creates another vector of standard distribution
matriza1= cbind(vector1, vector2) #Binds the two vectors
# 2. Math equations
# On the Vectors
print("Vector 1 Multiplication by 2" + vector1 * 2)
# Introduction to R
# Week 5
# Assignment by Nava Peter (211311147)
# 1. Creation of Vectors and Merging
vector1= rnorm(15, mean=0, sd=1) # Creates a vector of a standard distribution
vector2= rnorm(15, mean=0, sd=1) #Creates another vector of standard distribution
matriza1= cbind(vector1, vector2) #Binds the two vectors
# 2. Math equations
# On the Vectors
print("Vector 1 Multiplication by 2" + vector1 * 2)
print("Matrix 2: Column 1" + vector_col_1_m2)
# Introduction to R
# Week 5
# Assignment by Nava Peter (211311147)
# 1. Creation of Vectors and Merging
vector1= rnorm(15, mean=0, sd=1) # Creates a vector of a standard distribution
vector2= rnorm(15, mean=0, sd=1) #Creates another vector of standard distribution
matriza1= cbind(vector1, vector2) #Binds the two vectors
# 2. Math equations
# On the Vectors
print("Vector 1 Multiplication by 2" + vector1 * 2)
# Introduction to R
# Week 5
# Assignment by Nava Peter (211311147)
# 1. Creation of Vectors and Merging
vector1= rnorm(15, mean=0, sd=1) # Creates a vector of a standard distribution
vector2= rnorm(15, mean=0, sd=1) #Creates another vector of standard distribution
matriza1= cbind(vector1, vector2) #Binds the two vectors
# 2. Math equations
# On the Vectors
print(vector1 * 2) # Multiplication
print(vector1 / 2) # Division
print(vector1-2) # Subtraction
print (vector1 + 2) # Addition
# Between both tables
matriza2= matrix(rnorm(30, mean=0, sd=1), nrow= 15) #Creates a matrix that's the same size as the first matrix
print(matriza1 * matriza2) # Multriplication
print(matriza1 / matriza2) # Division
print(matriza1-matriza2) # Subtraction
print (matriza1 + matriza2) # Addition
# 3. Accessing and Changing Values in a Matrix
print(matriza1[4, 2]) #Prints the original value
matriza1[4, 2] = 0.79 #Changes the value
print(matriza1) # Prints the matrix with the new value
vector_row_5_m2= matriza2[5, ] # Creates a vector with the 5th row of matriza2
vector_col_1_m2 = matriza2 [ , 1] # Creates a vector with the value of the first column in matriza2
print(vector_row_5_m2) # Vector based on Row 5 of matriza2
print(vector_col_1_m2) # Vector based on Column 1 of matriza2
# 4. Statistical Analysis of Matrices
print (rowMeans(matriza1)) # Prints the means of all the rows in the first matrix
print (rowSums(matriza1)) # Prints the sums of all the rows
print(colMeans(matriza1)) # Prints the means of all the columns
print(colSums(matriza1)) # Prints the sums of all the columns
col_means = colMeans(matriza1)                # Mean of every column
shonut_cols = colSums((matriza1 - col_means)^2) # Calculates the shonut of each value in Matriza1
n_elements = length(matriza1)                   # Calculates the number of elements
idx_na = sample(1:n_elements, 4)                # Chooses 4 random places
matriza1[idx_na] = NA                           # Inserts NA in the random places we chose
count_NA= sum(is.na(matriza1)) # Counts the numbers of NAs
print(count_NA)
# 5. Matrix Transformation
df_m1= as.data.frame(matriza1) # Transforms matriza1
x1 = 2
x2 = 3
Multiplication <- function(x1, x2) {
x3 = 1000
y = x1 * x3
return(y)
}
print(Multiplication(x1, x2))
Multiplication <- function(x1, x2) {
x3 = 1000
browser()     # עצירה
y = x1 * x3
return(y)
}
Multiplication(x1, x2)
X3
x3
x1
x2
x3
Multiplication <- function(x1, x2) {
x3 = 1000
browser()     # עצירה
y = x1 * x3
return(y)
}
Multiplication(x1, x2)
x3
rm(list = ls())
x1 = 2
x2 = 3
Multiplication(x1, x2)
Multiplication <- function(x1, x2) {
x3 = 1000
browser()     # עצירה
y = x1 * x3
return(y)
}
rm(list = ls())
x1 = 2
x2 = 3
Multiplication(x1, x2)
files_names <- dir("stroop_data")   # Creates a variable that is called "files_names" that gets its information from the data in the computer directory
df <- data.frame()           #  creates an empty dataframe called df
for (f in files_names) { # Creates a variable called f that represents an individual document in the file we transported
temp <- read.csv(paste0("stroop_data/", f))   # reads every document and saves it under the temp variable
df <- rbind(df, temp)                  # adds to the empty dataframe the temp document that was read
}
names(df)
setwd("C:/Users/USER/OneDrive - mail.tau.ac.il/University/coding/r/Lesson 6")
names(df)
# Introduction to R
# Week 5
# Assignment by Nava Peter (211311147)
# A. Loading and Combining the Different Files
# 1. Using For Loop for That
files_names <- dir("stroop_data")   # Creates a variable that is called "files_names" that gets its information from the data in the computer directory
df <- data.frame()           #  creates an empty dataframe called df
for (f in files_names) { # Creates a variable called f that represents an individual document in the file we transported
temp <- read.csv(paste0("stroop_data/", f))   # reads every document and saves it under the temp variable
df <- rbind(df, temp)                  # adds to the empty dataframe the temp document that was read
}
names(df)
subject_num <- length(df).sum()
subject_num <- length(df)
subject_num <- unique(df)
subject_num <- length(df)
# Introduction to R
# Week 5
# Assignment by Nava Peter (211311147)
# A. Loading and Combining the Different Files
# 1. Using For Loop for That
files_names <- dir("stroop_data")   # Creates a variable that is called "files_names" that gets its information from the data in the computer directory
df <- data.frame()           #  creates an empty dataframe called df
for (f in files_names) { # Creates a variable called f that represents an individual document in the file we transported
temp <- read.csv(paste0("stroop_data/", f))   # reads every document and saves it under the temp variable
df <- rbind(df, temp)                  # adds to the empty dataframe the temp document that was read
}
names(df)
subject_num <- length(df)
subject_num <- length(unique(df$subject))
# Introduction to R
# Week 5
# Assignment by Nava Peter (211311147)
# A. Loading and Combining the Different Files
# 1. Using For Loop for That
files_names <- dir("stroop_data")   # Creates a variable that is called "files_names" that gets its information from the data in the computer directory
df <- data.frame()           #  creates an empty dataframe called df
for (f in files_names) { # Creates a variable called f that represents an individual document in the file we transported
temp <- read.csv(paste0("stroop_data/", f))   # reads every document and saves it under the temp variable
df <- rbind(df, temp)                  # adds to the empty dataframe the temp document that was read
}
names(df)
subject_num <- length(unique(df$subject))
subject_condition <- length(unique(df$condition))
#4. Creates a Histograma for Reaction Times for Congruent and Incongruent Conditions
names(df$condition) # returns the names of conditions in condition
#4. Creates a Histograma for Reaction Times for Congruent and Incongruent Conditions
columns.names(df$condition) # returns the names of conditions in condition
#4. Creates a Histograma for Reaction Times for Congruent and Incongruent Conditions
col.names(df$condition) # returns the names of conditions in condition
#4. Creates a Histograma for Reaction Times for Congruent and Incongruent Conditions
colnames(df$condition) # returns the names of conditions in condition
#4. Creates a Histograma for Reaction Times for Congruent and Incongruent Conditions
rows.names(df$condition) # returns the names of conditions in condition
# Introduction to R
# Week 5
# Assignment by Nava Peter (211311147)
# A. Loading and Combining the Different Files
# 1. Using For Loop for That
files_names <- dir("stroop_data")   # Creates a variable that is called "files_names" that gets its information from the data in the computer directory
df <- data.frame()           #  creates an empty dataframe called df
for (f in files_names) { # Creates a variable called f that represents an individual document in the file we transported
temp <- read.csv(paste0("stroop_data/", f))   # reads every document and saves it under the temp variable
df <- rbind(df, temp)                  # adds to the empty dataframe the temp document that was read
}
#2. Counts the numbers of participants and conditions
names(df) # checks the names of columns in df
subject_num <- length(unique(df$subject)) # Checks the numbers of subjects
subject_condition <- length(unique(df$condition)) # Checks the number of conditions in Condition
print(subject_num) # Prints the numbers of subjects
print(subject_condition) #Prints the numbers of Conditions
#3. Numbers and Percentage of NaN
count_NA_rt <- sum(is.na(df$rt)) # Counts the numbers of NAs in rt
print(count_NA_rt) # prints the number of conditions in rt
percentage_NA_rt <- count_NA_rt/length(df$rt)*100 # Calculates the percentage of NAs in rt
print(percentage_NA_rt) # Prints out the numbers of NAs in rts
#4. Creates a Histograma for Reaction Times for Congruent and Incongruent Conditions
unique(df$condition) # returns the names of conditions in condition
